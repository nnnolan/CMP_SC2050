#include <stdlib.h>
#include <stdio.h>

typedef struct nstruct {
    int key;
    struct nstruct *next;
} Node;


int main() {
    Node myNode, *yourNode;

    Node *p = NULL;
    

    int myKey = (*p).key;

    return 0;
}